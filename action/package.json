{
  "name": "install-musl-toolchain",
  "version": "1.0.0",
  "description": "GitHub Action to install the Elide musl toolchain for WHIPLASH",
  "main": "main.ts",
  "type": "module",
  "scripts": {
    "build": "bun build main.ts --outdir dist --target node",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/tool-cache": "^2.0.2",
    "@actions/io": "^1.1.3"
  },
  "devDependencies": {
    "@types/bun": "^1.3",
    "typescript": "^5.8.3"
  }
}
